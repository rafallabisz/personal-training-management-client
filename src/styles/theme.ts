import { createMuiTheme } from "@material-ui/core/styles";

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#3f51b5"
    },
    secondary: {
      main: "#e53935"
    },
    error: {
      main: "#d32f2f"
    },
    background: {
      default: "#fff"
    },
    common: {
      white: "#fff"
    }
  }
});
